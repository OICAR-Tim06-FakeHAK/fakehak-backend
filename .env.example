# Local .env template (do not commit with real secrets)

# JWT
JWT_SECRET=fakehak-local-development-secret-change-this-before-deploying
JWT_EXPIRATION_MS=86400000

# Database
DB_URL=jdbc:h2:mem:fakehak
DB_USER=sa
DB_PASSWORD=

# Active Spring profile
SPRING_PROFILES_ACTIVE=dev
